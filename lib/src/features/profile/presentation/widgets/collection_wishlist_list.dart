import 'package:flutter/material.dart';

class CollectionWishlistList extends StatelessWidget {
  const CollectionWishlistList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("No Data available"),
    );
  }
}
