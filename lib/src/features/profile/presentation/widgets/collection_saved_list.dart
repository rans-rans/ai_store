import 'package:flutter/material.dart';

class CollectionSavedList extends StatelessWidget {
  const CollectionSavedList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("No Data available"),
    );
  }
}
