import 'package:flutter/material.dart';

class CollectionFollowingList extends StatelessWidget {
  const CollectionFollowingList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("No Data available"),
    );
  }
}
